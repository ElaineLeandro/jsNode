/* Estrutura de condicional:else
 * Podemos execultar um outro bloco de código, caso a instrução do if não seja atendida
 * Ou seja, SE NÃO EXECUTAR o bloco if, EXECUTE o bloco else
 * Que resulta em um boolean (true or false)
 * 
 * let nome = 'Pedro'
 * 
 * if(nome == 'João'){
 * console.log('Seu nome é João')
 * }else{
 * console.log('Seu nome não é João')
 * }
}
*/

let velocidade = 15;
if(velocidade <= 80){
  console.log('Não foi multado!')
}else{
  console.log("Foi multado!")
}

velocidade(15)