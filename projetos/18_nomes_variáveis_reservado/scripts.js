/**Nomes de Variáveis reservados
 * 
 * Alguns nomes não podem ser utilizados para iniciar váriaveis;
 * breack case catch class const continue debugger default delete do else enum export extends false finally for function if implements import interface in instanceof let new package private protected public return static super switch this throw true try typeof var void while with yield
 * 
 * Ex: let breack ="parar"
 * Prém podemos usar elas + algumas palavras/digito;
 * Ex let breakElaine = 2;
*/

// let if = "teste"

// let function = "teste"

let functionTeste = "teste"

console.log(functionTeste);